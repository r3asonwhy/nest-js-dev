commands:
  01_create_database:
    command: |
      mysql -u root -e "CREATE DATABASE template-db;"
      mysql -u root -e "CREATE USER 'root'@'%' IDENTIFIED BY 'root';"
      mysql -u root -e "GRANT ALL PRIVILEGES ON template-db.* TO 'root'@'%';"
      mysql -u root -e "FLUSH PRIVILEGES;"
